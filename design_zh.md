

# 游戏配表自动化检查框架设计与Demo说明

## 1. 背景

在游戏项目中，静态配置表（如 Excel、CSV、JSON、XML 等）是逻辑实现的重要基础，例如道具表、任务表、奖励表、活动表等。  
但在实际开发中，配表错误非常常见，例如字段类型错误、数值范围越界、主键重复、多表引用不一致，甚至业务逻辑冲突。这些问题如果依赖人工检查或功能回归测试发现，通常反馈较晚，排查成本较高。

因此，本方案旨在设计一个自动化配表检查框架，将问题尽量前置到开发和提测阶段，降低人工成本并提升反馈效率。

## 2. 设计目标

本框架的目标包括：

- 支持读取静态配表数据
- 支持定义和执行多种校验规则
- 能够准确输出问题定位信息
- 具备一定的扩展性和工程集成能力
- 可进一步接入 CI/CD 流程

## 3. 核心架构设计

本框架整体分为四层：

### 3.1 输入层
输入层负责读取不同格式的配表文件，并将其转换为统一的数据结构，供后续规则执行使用。  
当前 Demo 支持 CSV，后续可扩展支持 Excel、JSON、XML 等格式。

### 3.2 规则层
规则层负责定义要检查的规则。  
当前 Demo 中，规则通过 `rules/rules.json` 配置文件定义，而不是写死在代码中。这样做可以提升维护性和适配效率。

当前支持的规则类型包括：

- 字段类型检查
- 数值范围检查
- 唯一性检查
- 外键关联检查

### 3.3 校验执行层
校验执行层负责根据规则类型分发到不同 validator 执行具体检查。  
当前 Demo 包含以下校验器：

- 类型检查
- 范围检查
- 唯一性检查
- 外键检查

### 3.4 输出层
输出层负责将问题结果结构化输出。  
当前 Demo 支持：

- 控制台输出
- JSON 报告输出

每条错误至少包含：

- 表名
- 行号
- 字段名
- 规则类型
- 错误详情

## 4. 规则抽象设计

为了兼顾通用性和扩展性，本方案采用“规则配置化”的方式。  
通用规则通过 JSON 配置描述，例如：

```json
{
  "type": "range",
  "field": "price",
  "min": 0,
  "max": 999999
}
```

这种方式适合表达大部分常见校验需求，同时使框架更容易适配不同项目。

在完整版本中，规则还可以进一步分为：

- 单表规则
- 多表关联规则
- 自定义业务逻辑规则

其中复杂业务逻辑可通过插件方式扩展。

## 5. 性能与扩展性考虑

当配表数据量较大时，可从以下几个方向进行优化：

- 使用哈希结构提升唯一性和外键检查效率
- 按表进行并发执行
- 在 CI 中仅对改动表和关联表进行增量校验
- 对超大数据文件采用分块或流式读取

对于新项目适配，可将差异放在输入适配器、schema 配置和规则配置层，而保持主校验引擎不变，从而实现快速迁移。

## 6. CI/CD 集成思路

该框架可集成到以下开发流程节点：

- 本地 pre-commit / pre-push 快速检查
- CI 流水线自动校验
- 发布前全量校验

校验失败时，可输出精确定位信息，例如表名、行号、字段名和错误描述。
在真实项目中，还可以进一步结合 Git 提交人信息、表负责人配置、飞书/邮件通知等机制，实现问题自动分发。

## 7. Demo说明

本次 Demo 实现了一个最小可运行版本，支持：

- CSV 输入
- JSON 规则配置
- 字段类型检查
- 数值范围检查
- 唯一性检查
- 外键检查

虽然功能较简化，但已经覆盖了自动化配表检查的核心流程，即：

输入读取 → 规则解析 → 执行校验 → 输出报告

## 8. 运行方式

```
python main.py
```

## 9. 后续可扩展方向

- 支持 Excel / JSON / XML
- 引入 Schema 配置
- 增加自定义业务规则插件机制
- 生成 HTML 报告
- 接入 GitHub Actions / Jenkins
- 增加责任人映射和通知机制